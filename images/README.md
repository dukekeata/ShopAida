Drop the logo mark image file here as `logo-mark.png`.

- Use the mark-only PNG provided by you (no wordmark). Save it as:
  - `images/logo-mark.png` for pages in the site root (index, categories, cart, login, register)
  - `../images/logo-mark.png` is referenced in files under `product/` (generator uses this path)

If you'd like I can convert the image to an inline SVG (better scaling) if you provide an SVG file.

Tip: Put a 400px wide PNG or an SVG for best quality; the site will scale it down (CSS sets 42px height on desktop).